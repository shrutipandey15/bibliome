# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
venv/
env/

# Environment variables. The bare pattern matches at any depth, but
# deploy/env.production is spelled out because it was committed once anyway —
# it carried a live VAPID private key until the history was rewritten.
.env
env.production
deploy/env.production

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Docker
docker-compose.override.yml

# Personal reading data. scripts/import_reading_journal.py takes --file, so the
# spreadsheet it reads never needs to live in the repo — and one did, publicly,
# until it was removed.
*.xlsx